spring.application.name=lima-salud
# Railway (y otros PaaS) inyectan PORT; local sigue siendo 8080 por defecto.
server.port=${PORT:8080}
spring.thymeleaf.cache=false
spring.web.resources.cache.period=0
